# 🚀 Быстрый старт - Деплой ED Analytics

## Рекомендуемый способ (самый быстрый):

```bash
./scripts/deploy-fast.sh
```

**Время деплоя: ~2-4 минуты**

---

## Альтернативный способ:

```bash
./scripts/deploy.sh
```

**Время деплоя: ~3-5 минут**

---

## Полная пересборка (когда изменились зависимости):

```bash
FULL_REBUILD=true ./scripts/deploy-fast.sh
```

---

## Быстрая проверка после деплоя:

```bash
# Проверка frontend
curl http://130.193.46.4

# Проверка backend
curl http://130.193.46.4:5000/health

# Проверка brama
curl http://130.193.46.4:5001/health
```

---

## Просмотр логов:

```bash
ssh -i ~/.ssh/llm-cpu/appuser-ed25519 appuser@130.193.46.4
cd ed_analytics
docker compose -f docker-compose.prod.yml logs -f
```

---

## Что изменилось?

### ❌ Было (МЕДЛЕННО ~15 минут):
- Полное удаление папки проекта
- Удаление всех Docker образов
- Пересборка без кэша
- Копирование всех файлов

### ✅ Стало (БЫСТРО ~2-4 минуты):
- Обновление только изменённых файлов (rsync)
- Использование Docker кэша
- Сохранение образов между деплоями
- Умная инкрементальная сборка

---

📖 **Подробная документация**: `scripts/DEPLOY_OPTIMIZATION.md`

